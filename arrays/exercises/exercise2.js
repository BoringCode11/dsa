/*
  ! Exercise 2
  ? Store set of words in an array and display the contents both forwards and backwards
*/

const names = [
  'James', 'Lesedi', 'Abdul', 'Zandi', 'Cynthia'
]

const backwards = names.sort().reverse()
// console.log(backwards);

const forwards = names.sort()
// console.log(forwards);